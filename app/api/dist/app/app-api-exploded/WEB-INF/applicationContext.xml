<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:util="http://www.springframework.org/schema/util"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
                           http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util.xsd">

    <context:property-placeholder location="classpath:application.properties" ignore-unresolvable="true"/>

    <context:component-scan base-package="sgw.common"/>

    <bean id="jedisPoolConfig" class="redis.clients.jedis.JedisPoolConfig">
        <property name="maxTotal" value="${app.easycache.pool.maxTotal:16}"/>
        <property name="maxIdle" value="${app.easycache.pool.maxIdle:8}"/>
        <property name="minIdle" value="${app.easycache.pool.minIdle:0}"/>
    </bean>

    <bean id="jedisPool" class="redis.clients.jedis.JedisPool" destroy-method="close">
        <constructor-arg ref="jedisPoolConfig"/>
        <constructor-arg value="${app.easycache.host:localhost}"/>
        <constructor-arg value="${app.easycache.port:6379}"/>
        <constructor-arg value="${app.easycache.timeout:2000}"/>
        <constructor-arg value="${app.easycache.password:}"/>
    </bean>

    <bean id="rabbitConnectionFactory" class="org.springframework.amqp.rabbit.connection.CachingConnectionFactory">
        <constructor-arg value="${app.rabbit.host:localhost}"/>
        <property name="port" value="${app.rabbit.port:5672}"/>
        <property name="username" value="${app.rabbit.username:guest}"/>
        <property name="password" value="${app.rabbit.password:guest}"/>
    </bean>

    <bean id="rabbitTemplate" class="org.springframework.amqp.rabbit.core.RabbitTemplate">
        <constructor-arg ref="rabbitConnectionFactory"/>
    </bean>

    <!-- 공통 인프라 설정 스캔 (JedisPool, RabbitMQ 등) -->
    <context:component-scan base-package="sgw.common.infra.config,sgw.common.infra.condition"/>

</beans>
