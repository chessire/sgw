<?xml version="1.0" encoding="UTF-8"?>
<project name="common-classpath">
  <property file="${root.dir}/build.properties"/>

  <path id="shared.compile.classpath">
    <pathelement location="${build.dir}/common-core/classes"/>
    <pathelement location="${build.dir}/common-infra/classes"/>
    <pathelement location="${build.dir}/common-web/classes"/>
    <path refid="project.classpath"/>
  </path>
</project>
